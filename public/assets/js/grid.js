$(function () {
  //get size of screen
  var widths = $(window).width();

  var grids = $('.grid');
  grid.css('position', 'relative');
  grid.css('width', widths);

  var gridItem = $('.grid__item');

  console.log(gridItem);
});